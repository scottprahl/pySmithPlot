version: 2

build:
  os: "ubuntu-22.04"  # or another supported OS image
  tools:
    python: "3.11"    # or your desired Python version

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt
